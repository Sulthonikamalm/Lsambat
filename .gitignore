# ── KUNCI: jangan pernah commit token API ──
.env
.env.local
.env.*.local

# ── Python ──
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# ── Editor ──
.vscode/
.idea/

# ── Data: hasil scrape boleh di-track (untuk shared dataset demo) ──
# .env tetap dilindungi oleh aturan di atas — tidak akan ikut walau di data/
data/_archive/
